# LLM 활용 논문작성 지침

> 이 문서는 국회예산정책처 「생성형 AI를 활용한 보고서 작성법 교육」(박주이, 2026.8.4) 강의 자료의 설계 원리(에이전트·검증 게이트·하네스 개념)를 학습하여, **학술 논문 작성**에 맞게 재구성한 실무 지침이다. 이 저장소의 실증분석(Oster bounds, CRE/Mundlak, HT-IV 등)을 논문으로 옮기는 작업에 바로 적용할 수 있도록 작성했다.

## 0. 핵심 전제 — 왜 이 지침이 필요한가

LLM은 두 가지 상반된 특성을 동시에 가진다.

- **기대**: 초안 작성·문헌 요약·교정 같은 반복 작업을 위임하면 전문성 있는 판단에 집중할 시간이 늘어난다.
- **불신의 근거**: LLM이 만드는 가장 위험한 오류는 "없는 숫자를 지어내는 것"이 아니라 **"그럴듯하지만 다른 출처·다른 지표의 값을 가져다 쓰는 것"**이다. 이런 오류는 확정적인 어조로 제시되기 때문에 사람의 검토를 그대로 통과한다.

따라서 이 지침의 목표는 "프롬프트를 잘 쓰는 법"이 아니라, **논문 작성을 파이프라인(공정)으로 설계하여 검증을 구조에 박아 넣는 법**이다. 매번 다시 설명하는 챗봇 사용에서, 규칙·절차·검증 기준을 파일로 고정해 재사용하는 시스템 사용으로 전환하는 것이 핵심이다.

| | 챗봇에 매번 시키기 | 시스템(에이전트 팀)으로 만들기 |
|---|---|---|
| 품질 | 그날그날 다름, 재현 불가 | 파일로 고정된 표준을 반복 재현 |
| 검증 | 작성자 본인이 스스로 확인(자기검토) | 초안을 쓴 적 없는 독립 검증자가 확인 |
| 기록 | 세션 종료 시 소멸 | 설계·근거·검수 기록이 파일로 남아 인수인계·재현 가능 |

핵심 질문은 항상 같다 — **"그럴듯하게 틀린 숫자를, 나는 잡아낼 수 있는가?"**

---

## 1. 논문 한 편을 5인 팀으로 쪼갠다

LLM 하나에게 "논문 써줘"라고 시키는 대신, 역할을 쪼개 **오케스트레이터 + 4개 전문 역할**로 나눈다. 원본 강의의 `policy-research-kit`/`socsci-paper-kit` 구조를 논문 작성용으로 번안하면 다음과 같다.

| 역할 | 맡는 일 | 논문 작성에서 하는 일 |
|---|---|---|
| **조율자 (orchestrator)** | 한 마디를 받아 아래 넷을 순서대로 호출·조율 | 사람이 유일하게 말을 거는 창구. "지방재정 건전성 논문 초안 써줘" |
| **설계자 (designer)** | 의도 분석·연구질문·목차(IMRaD) 설계 | 연구질문·가설·식별전략·목차를 먼저 제시 — **관문①의 대상** |
| **조사관 (investigator)** | 문헌·통계·선행연구 조사, 모든 사실에 출처 병기 | 선행연구 요약, 데이터 출처·정의 확인, 인용 근거 장부 작성 |
| **작성자 (writer)** | 논증형 본문 집필 | Introduction–Literature–Data/Method–Results–Discussion 집필 |
| **검수자 (reviewer)** | 설계 관문 + 초안 검수, **초안을 쓴 적 없는 별도 컨텍스트**에서 실행 | 논리·근거·통계적 타당성·출처 실재의 4~5축 검수 — **관문②** |

**왜 검증자를 분리하는가**: 초안을 쓴 사람(또는 세션)이 자기 결과를 스스로 통과시키게 하면 안 된다. 기관의 상호검토(peer review) 문화를 그대로 AI 작업 흐름에 옮긴 것이며, 단일 세션 자기점검으로는 구조적으로 불가능한 교차 검증을 가능하게 한다.

### 짜임 세 가지
- **순차형**: 조사 → 집필 → 검수처럼 앞이 끝나야 뒤가 진행
- **병렬형**: 문헌조사·데이터조사를 동시에 진행해 속도를 2~3배 높임
- **계층형**: 오케스트레이터가 전체를 조율·승인

논문 작성 팀은 보통 **계층형 + 순차형**(설계→조사→집필→검수) 구조가 되고, 조사 단계 내부(선행연구/데이터/통계기법)는 병렬로 돌릴 수 있다.

---

## 2. 관문(Gate) 두 개가 심장이다

```
지시 "○○ 논문 써줘" (사람: 한 줄)
   │
   ▼
[1] 연구설계 (연구질문·가설·식별전략·목차)
   │
   ▼
[관문①] 설계 검토 — GO / NO-GO 판정
   │
   ▼
★ 목차·연구설계 승인 ── 사람이 결정 ★
   │
   ▼
[2] 근거 조사 (출처 병기)
   │
   ▼
[3] 집필 (논증형 본문)
   │
   ▼
[관문②] 검수 — 다축 검수, 수정요청서 산출
   │
   ▼
[4] 최종본 (+ 필요 시 서식 변환)
```

- **관문①(설계 승인)**: 잘못된 연구설계·식별전략으로 조사·집필을 진행하는 것이 가장 비싼 헛작업이다. **집필 전에** 반드시 사람이 연구질문·식별전략·목차를 승인한다.
- **관문②(검수)**: 초안이 나오면 아래 축으로 독립 검수한다.
  1. 논리 정합성 — 서론의 질문과 결론의 답이 일치하는가
  2. 근거 충실성 — 모든 핵심 주장에 출처가 붙어 있는가
  3. 방법론 타당성 — 식별전략·통제변수·강건성 점검이 선행연구 관행과 부합하는가
  4. 정량성 — 표·계수·표준오차가 본문 서술과 일치하는가 (수치 대사)
  5. **출처 실재 검증** — 인용된 문헌·데이터가 실제로 존재하고, 인용한 내용이 그 문헌에 실제로 있는가

두 관문 사이, **사람의 개입 지점은 "목차/설계 승인" 한 번**으로 최소화한다. 자동화 한가운데 의도적으로 심어 둔 사람의 결정 지점이며, 여기서 방향이 틀리면 이후 전부가 헛일이 된다.

---

## 3. 검증자의 3단 프로토콜 — "의심"을 절차화한다

검증자(또는 검증 단계)는 다음 순서를 항상 지킨다.

1. **출처 등급 분류(Tier)**
   - Tier 1: 원 데이터 제공기관·1차 사료·저자가 직접 접근한 데이터셋
   - Tier 2: 동료심사 학술지·국책연구기관 보고서
   - Tier 3: 주요 학술 프리프린트·컨퍼런스 페이퍼
   - Tier 4: 블로그·SNS·비검증 요약 — **단독 근거로 인용 불가**, 반드시 상위 등급으로 교차 확인
2. **교차 일관성 확인** — 재인용 사슬(A가 B를 인용, B가 C를 인용하는데 실은 같은 원 출처의 재탕)인지, 수치·연도·표본크기가 서로 다른 인용 간에 상충하지 않는지 대조
3. **실존 확인** — 인용한 DOI·URL·데이터셋 버전이 실제로 열리는지, 인용된 페이지·표에 그 수치가 정말 있는지 확인

기본 태도는 **회의론자**다. "AI가 만들었을 가능성"을 항상 상수로 둔다. 특히 **매끄럽고 확정적인 어조의 문장일수록 더 의심**한다 — 그럴듯함과 정확함은 다른 것이다. 불확실하면 관대하게 넣지 말고, 엄격하게 빼거나 `[확인 필요]` 태그를 남긴다.

> 실측 사례(원 강의 부록): 같은 요청을 순정 LLM 단독 vs 위와 같은 검증 체계를 갖춘 팀에 주고 제3자가 인용을 전수 열람했을 때 — 순정 단독은 수치·귀속 오류가 발생하고 출처 부착률이 낮았던 반면, 검증 체계를 갖춘 결과물은 오류 0건·출처 부착률 100%·검증 과정 기록(5종 파일)이 남았다. **문장 유려함은 순정 쪽이 오히려 나을 수 있다 — 차이는 "오류를 잡는 장치가 있는가"에서 난다.**

---

## 4. 다섯 부품으로 나만의 논문작성 에이전트를 조립한다

| 부품 | 역할 | 논문 작성 예시 |
|---|---|---|
| **CLAUDE.md** (또는 프로젝트 규칙 파일) | 항상 읽히는 "연구실 매뉴얼" — 역할·규칙·금지사항 | "인용은 APA 7판 · 회귀표는 표준오차를 괄호로 · 인과적 언어(causes, effect) 사용 전 식별전략 명시 · 원자료 대조 없이 수치 인용 금지" |
| **스킬 (Skill / SOP)** | 불러 쓰는 작업 절차서 — 반복 작업의 표준 | "문헌 요약 절차", "강건성 점검표 생성", "교정교열(`paper-proofread`류)" |
| **슬래시 명령어** | 자주 쓰는 절차의 단축키 | `/lit-review 소유구조 감소 문헌` → 검색→요약→표 생성까지 한 줄 |
| **서브에이전트** | 독립 컨텍스트의 팀원 — 병렬·전문화 | 문헌조사 담당과 통계결과 검증 담당을 별도로 |
| **훅 (Hook)** | 정해진 시점의 자동 규칙 — 잊어도 지켜짐 | 저장할 때마다 표 계수와 코드 출력 대조, 원본 데이터 파일 수정 시도 자동 차단 |

이 다섯 부품의 합을 **하네스(harness)**라 부른다 — 모델은 "인재", 하네스는 "연구실의 제도"에 해당한다. 같은 모델이라도 어떤 도구를 쥐여주고 무엇을 검증하게 하느냐에 따라 산출물의 신뢰도가 갈린다. 논문작성 경쟁력은 모델 선택이 아니라 **이 판(하네스)을 얼마나 잘 설계하느냐**에서 나온다.

### CLAUDE.md 예시 골격 (연구 프로젝트용)

```markdown
# 연구 프로젝트 규칙

## 역할
소유구조 감소 모형(H3) 관련 실증논문 작성을 보조한다.

## 표기·서식
- 계수 옆 괄호는 표준오차, 유의수준은 *** p<0.01, ** p<0.05, * p<0.1
- 모든 회귀표는 관측치 수·R²·고정효과 포함 여부를 마지막 행에 명시

## 절대 규칙
- 코드 출력(회귀 결과)에 없는 계수·표준오차를 본문에 쓰지 않는다
- 인과적 표현(causes, leads to, effect of)은 식별전략(도구변수·강건성 검정)을 
  먼저 서술한 이후에만 사용한다
- 인용은 실제로 열람한 문헌에 한정한다. 열람하지 못했으면 [원문 미확인] 표시

## 금지
- .do/.py 원본 스크립트를 임의로 수정해 결과를 재생산하지 않는다
  (수정이 필요하면 별도 파일로 분리하고 사유를 기록)
```

---

## 5. 원자료·문헌에 "손발"을 붙인다 — API/MCP, 그리고 문서 파싱

LLM이 학습된 기억만으로 답하면 철 지난 수치·재구성된 인용을 낼 위험이 크다. **오늘자 원자료에 직접 접근하는 통로**를 붙여야 "그럴듯한 오류"의 발생 여지가 줄어든다.

- **API**: 통계청·한국은행 ECOS·KOSIS 등 기계가 직접 읽는 창구. 사람이 홈페이지에서 눈으로 보고 옮겨 적던 것을 기계가 그대로 받아온다.
- **MCP(Model Context Protocol)**: 서로 다른 API·도구를 AI 쪽에서 표준 규격으로 통일한 연결 방식. 공식 MCP가 없는 자료원은 간단한 래퍼를 직접 만들 수 있다(자산으로 남는다).
- **문서 파싱**: 논문 초안(.docx)·선행연구 PDF·데이터 코드북을 표·수치 데이터로 구조화하는 도구를 앞단에 둔다. 원본은 그대로 보존하고, AI는 변환된 텍스트본으로 작업한다(**원천 분리 원칙**).

**API 키·비밀정보 관리 원칙**
- 인증키는 코드·문서·채팅창에 직접 쓰지 않는다. `.env` 파일에 `이름=값`으로 저장하고 `.gitignore`에 `.env`를 추가해 저장소에 올라가지 않게 한다.
- 미공개 데이터·개인정보가 포함된 자료는 처음부터 작업 대상에서 제외한다(공개 자료만 사용).

---

## 6. 다섯 가지 실행 태도

1. **자연어로 목적을 말하듯 지시한다** — "코드 짜줘"(✗) 대신 "이 강건성 점검을 하고 싶고, 제약은 이렇다"(✓)로 목적·제약·기대결과를 문장으로 전달한다.
2. **한 번에 완성하려 하지 않는다** — 첫 결과물은 초안이다. 대화를 이어가며 조각내 개선한다.
3. **작은 표본으로 먼저 검증한다** — 전체 데이터셋에 돌리기 전에 일부 표본·소규모 케이스로 예상대로 동작하는지 확인한 뒤 확장한다.
4. **검증은 필수, 할루시네이션은 상수로 취급한다** — 숫자·인용·통계치는 원문·원자료 재확인이 기본값이다. 그럴듯함과 정확함을 혼동하지 않는다.
5. **민감정보는 처음부터 분리한다** — 미공개 데이터·개인정보는 시작 단계에서 제외하고, API 키는 환경변수로 분리 보관한다.

---

## 7. 나만의 논문작성 에이전트 기획 템플릿

기존 킷을 개조하거나 처음부터 새로 만들 때, 아래 9항목을 채우면 그대로 개발 명세가 된다. (이 프롬프트를 LLM에게 그대로 주고 "코드 구현은 하지 말고 기획안 md 파일 하나만 만들어 달라"고 요청하면 초안을 받을 수 있다.)

1. **개요** — 한 줄 정의 · 타겟(누구를 위한 것인가) · 해결할 문제 · 성공 기준
2. **범위** — 첫 버전에 포함/제외할 것 (제외 이유까지 명시)
3. **핵심 시나리오** — 입력(데이터/문헌/초안) → 처리 → 출력(문단/표/전체 초안)을 구체 예시로
4. **예상 병목** — 항목별 예상 문제와 대응 방안 (예: 인용 실재 확인이 느리다 → 배치 처리)
5. **구성요소 설계** — 규칙 파일(CLAUDE.md) 초안 · 스킬 목록(이름/역할/트리거) · 필요한 MCP/API(이름/용도/이유)
6. **하네스 설계** — 폴더 구조 · 도구/권한(금지 작업 명시, 예: 원본 데이터 수정 금지) · 피드백 루프 · 실패 시 처리 · 테스트 케이스 3개 이상
7. **워크플로우 & 서브에이전트** — 전체 흐름도(mermaid) · 역할별 입력/출력/사용 도구 표
8. **결과물 형태** — 무엇을(초안, 표, 검수 리포트) 어떤 형태로 산출하는지
9. **구현 순서** — 무엇부터 만들지 단계별 체크리스트

> 습관: 완성된 기획안을 채팅에 붙여넣지 말고 **파일(`기획안.md`)로 저장**한다. 다음 세션에서도 그대로 다시 읽히기 때문이다.

---

## 8. 이 저장소에 적용하는 첫걸음

1. 프로젝트 루트에 `CLAUDE.md`를 만들어 이 논문의 표기 규칙(변수명, 단위, 유의수준 표기, 인과 언어 사용 조건)을 적는다.
2. `oster_bounds_master_v5.do`, `session4a_mundlak.py`, `session4b_ht_iv.py` 각각의 출력(회귀표·강건성 점검 결과)을 **"근거 장부"** 형태로 별도 파일에 정리해, 본문 집필 시 그 장부만 참조하게 한다(본문과 근거를 분리 보관).
3. 문헌조사·본문집필·검수를 최소 세 개의 별도 대화(또는 서브에이전트)로 분리한다 — 특히 검수는 초안을 작성한 세션과 다른 세션에서 진행한다.
4. 초안이 나오면 관문②의 5축(논리·근거·방법론·정량성·출처실재)으로 체크리스트를 만들어 매 초안마다 반복 적용한다.
5. 작게 시작한다 — 한 개 섹션(예: Results 한 개 표)부터 이 파이프라인을 시험하고, 잘 작동하면 전체 논문으로 확장한다.
